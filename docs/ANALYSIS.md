# 📊 Analysis Report: N-Tier Architecture (Week 6)

## 1. System Overview
ใน Lab นี้ เราได้เปลี่ยนสถาปัตยกรรมจาก Client-Server ธรรมดา (Week 5) มาเป็น **N-Tier Architecture** ที่สมบูรณ์ โดยมีการแบ่ง Tier ทางกายภาพ (Physical Separation) ดังนี้:

| Tier | Component | Technology | Port | หน้าที่หลัก |
|------|-----------|------------|------|-------------|
| **Tier 1 (Web)** | Web Server | Nginx | 80, 443 | รับ Request, ทำ SSL Termination, และ Serve Static Files |
| **Tier 2 (App)** | Backend API | Node.js + Express | 3000 | ประมวลผล Business Logic และเชื่อมต่อฐานข้อมูล |
| **Tier 3 (Data)** | Database | PostgreSQL | 5432 | จัดเก็บข้อมูลแบบ Relational Database Management System (RDBMS) |

---

## 2. Architecture Comparison (การเปรียบเทียบสถาปัตยกรรม)

### เปรียบเทียบกับ Week 5 (Client-Server with SQLite)
| หัวข้อ | Week 5 (Client-Server) | Week 6 (N-Tier) | การวิเคราะห์ |
|--------|------------------------|-----------------|--------------|
| **Database** | SQLite (File-based) | PostgreSQL (Server-based) | PostgreSQL รองรับ Concurrent Write ได้ดีกว่า และเหมาะกับ Production |
| **Security** | HTTP (Plain Text) | HTTPS (Encrypted) | Week 6 ปลอดภัยกว่า เพราะมีการเข้ารหัสข้อมูลระหว่างทางด้วย SSL |
| **Entry Point** | Node.js (Port 3000) | Nginx (Port 443) | การมี Nginx ช่วยบังหน้าทำให้จัด Load Balance ได้ และ Node.js ไม่ต้องรับโหลด Static Files |
| **Scalability** | Scale ยาก (ติดที่ DB ไฟล์เดียว) | Scale ง่าย | สามารถแยก Database ไปอีกเครื่อง หรือเพิ่ม Node.js หลายตัวหลัง Nginx ได้ |

---

## 3. Technology Analysis (วิเคราะห์เทคโนโลยี)

### 3.1 ทำไมต้องใช้ Nginx (Reverse Proxy)?
1.  **Security:** ซ่อน Port 3000 ของ Backend ไว้ ไม่ให้เข้าถึงได้โดยตรง
2.  **SSL/TLS Termination:** Nginx จัดการเรื่องเข้ารหัส/ถอดรหัส HTTPS ได้เร็วกว่า Node.js
3.  **Performance:** Nginx เสิร์ฟไฟล์ Static (HTML/CSS/JS) ได้เร็วกว่า Node.js มาก

### 3.2 ความรู้สึกในการเปลี่ยนจาก SQLite เป็น PostgreSQL
* **ความยาก:** การติดตั้งซับซ้อนกว่า SQLite ที่แค่มีไฟล์ก็รันได้ ต้องมีการสร้าง User/Permission
* **ข้อดี:** มี Type ที่ชัดเจนกว่า (เช่น ENUM, TIMESTAMP) และรองรับ Constraint (CHECK title length) ในระดับ Database ทำให้ข้อมูลถูกต้องแม่นยำกว่า

---

## 4. ปัญหาที่พบและวิธีแก้ไข (Problems & Solutions)

### 4.1 Connection Refused (Database)
* **ปัญหา:** Node.js ไม่สามารถเชื่อมต่อ PostgreSQL ได้ในตอนแรก
* **สาเหตุ:** ไม่ได้สร้าง User หรือให้สิทธิ์ใน Database
* **แก้ไข:** ใช้คำสั่ง `CREATE USER` และ `GRANT ALL PRIVILEGES` ให้ถูกต้อง

### 4.2 Self-signed Certificate Warning
* **ปัญหา:** Browser ขึ้นเตือน "Not Secure" ตัวแดง
* **สาเหตุ:** ใช้ Certificate ที่สร้างเอง (Self-signed) ไม่ได้ออกโดย CA ที่เชื่อถือได้
* **แก้ไข:** กด Advanced -> Proceed ใน Browser (ในระบบจริงควรใช้ Let's Encrypt)

### 4.3 Git Nested Repository
* **ปัญหา:** ไม่สามารถ Commit งานได้เพราะมี .git ซ้อนกัน
* **แก้ไข:** ลบ .git ในโฟลเดอร์ลูก แล้วใช้ `git rm --cached` เพื่อเคลียร์สถานะ

---

## 5. สรุปผลการทดลอง (Conclusion)
การทำ N-Tier Architecture บน Single VM ช่วยให้เห็นภาพการทำงานของระบบระดับ Production ได้ชัดเจนขึ้น แม้จะรันบนเครื่องเดียวแต่การแยก Process และ Port ของแต่ละ Service (Nginx, Node, Postgres) ทำให้เข้าใจหลักการ Separation of Concerns และเตรียมพร้อมสำหรับการแยก Server จริงในอนาคต (Multi-VM)

## screenshot
### ./scripts/test-api.sh
<img width="614" height="402" alt="image" src="https://github.com/user-attachments/assets/70a8422e-d7de-4511-b6c6-1fac50156d84" />

### https://taskboard.local/
<img width="864" height="2495" alt="taskboard local_" src="https://github.com/user-attachments/assets/c328cd63-b1b7-45aa-ba85-9e8116bac4f4" />

---
## Author
**ผู้จัดทำ:** นางสาวกชพร วงศ์ใหญ่
**รหัสประจำตัว:** 67543210067-4
